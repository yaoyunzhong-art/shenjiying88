# 2026-06-24

## 21:12 Heartbeat — 发现回归

从 Pulse-58 (08:37, 全绿) 到 21:12 期间出现了测试回归:
- 全量 `@m5/api` 测试发现 **2 个根因错误 → 大量级联 failure**:
  1. **governance-approval**: `markExecuted` 不更新 status 字段 → `toApprovalSnapshot` 返回的任务状态仍是 `'APPROVED'` 而非 `'EXECUTED'` → 级联影响到 audit log 和 member service 测试
  2. **market locale**: `locale` 已从 string 重构为 object `{defaultLanguage, supportedLanguages}` → portal 视图测试仍期望 string → 级联影响所有 market role-extended 测试 (~10个fail)
- 上次检查是 Pulse-58 08:37，介于今天提交了多个前端/后端 commit，说明回归来源是今天白天的变更

未做修复 — 心跳仅监测。建议下一脉冲派树哥修两个根因。

## 23:24 Heartbeat — 深夜安静检查

### 系统状态
- ✅ 最近 commits (21:00后): 基础设施 Phase-13 (Redis + Cache), ESLint 修复, 以及树哥前端促销页面自动提交
- ✅ 有 stash 存在 (b70301b9 WIP on main)
- ✅ 深夜安静时段，无紧急事件

### 操作
- 深夜(23:00-08:00)安静模式，不打扰大飞哥
- 记录状态后 HEARTBEAT_OK
