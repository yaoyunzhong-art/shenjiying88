/**
 * E2E: Member 会员 HTTP 链路
 *
 * 链路:
 *   HTTP → TenantContext → TestController → MemberService
 *
 * 验证:
 *   - 会员注册 / 列表 / 详情
 *   - 积分增减 / 升级检查
 *   - 会员登录 / 会话查询
 *   - 跨租户隔离
 *   - LYT 会员快照读写
 *   - 会员状态调整 / 等级覆盖
 *   - 按条件过滤会员列表
 *   - 会员统计
 *   - 会员信息更新
 *   - 会员标签管理
 */
import 'reflect-metadata';
//# sourceMappingURL=member.e2e.test.d.ts.map