/**
 * metrics.controller.test.ts — MetricsController 单元测试
 *
 * 覆盖:
 *   - GET /metrics 路由装饰器存在
 *   - GET /healthz 路由装饰器存在
 *   - getMetrics 返回 Prometheus 文本格式
 *   - getHealth 返回 { status, metrics }
 */
export {};
//# sourceMappingURL=metrics.controller.test.d.ts.map