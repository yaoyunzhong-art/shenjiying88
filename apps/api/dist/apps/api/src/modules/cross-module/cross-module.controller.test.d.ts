/**
 * 🐜 自动: [cross-module] [D] controller spec 补全
 *
 * 补全内容：
 * - 正例（保留原测试）
 * - 反例（不存在链路的查询、无效状态）
 * - 边界测试（空链路、全 verified、全 broken 状态、entity 纯函数）
 *
 * 覆盖 entity 函数：
 * - toValidationSummary: 各种状态组合
 * - isAllVerified: 边界情况
 * - hasBrokenChain: 正反例
 */
import 'reflect-metadata';
//# sourceMappingURL=cross-module.controller.test.d.ts.map