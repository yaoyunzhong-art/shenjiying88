# System Config 系统配置

> 系统配置管理服务，支持全局配置的 CRUD 管理

## API
- GET /system-config — 配置列表
- GET /system-config/:key — 配置详情
- PUT /system-config/:key — 更新配置
- POST /system-config — 创建配置
- DELETE /system-config/:key — 删除配置
