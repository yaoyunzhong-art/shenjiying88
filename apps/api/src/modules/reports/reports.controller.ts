/**
 * reports.controller.ts — Reports Controller (canonical name)
 *
 * 🐜 V17: 模块补齐 — 规范文件名
 *
 * 委托至 report.controller.ts 中的 ReportController。
 */

export { ReportController } from './report.controller'
