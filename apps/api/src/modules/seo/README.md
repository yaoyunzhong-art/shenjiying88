# SEO 搜索引擎优化

> SEO 元数据管理服务，支持页面元数据与 Sitemap 管理

## 功能
- 页面元数据管理
- Sitemap 生成

## 依赖
- AgentModule

## API
- PUT /seo/metadata — 更新元数据
- GET /seo/metadata/:path — 查询元数据
- DELETE /seo/metadata/:path — 删除元数据
- GET /seo/metadata — 全部元数据
- POST /seo/sitemap — 生成 Sitemap
