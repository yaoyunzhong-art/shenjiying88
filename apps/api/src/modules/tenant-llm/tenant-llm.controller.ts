/**
 * tenant-llm.controller.ts — Tenant LLM Controller (canonical name)
 *
 * 🐜 V17: 模块补齐 — 规范文件名
 *
 * 委托至 llm-config.controller.ts 中的 TenantLLMController。
 */

export { TenantLLMController } from './llm-config.controller'
