# Tenant LLM 租户 LLM 配置

> 租户大模型配置管理服务，支持 LLM 配置的 CRUD

## 功能
- 租户 LLM 配置管理 (CRUD)
- 配置详情查询

## API
- GET /tenant-llm/configs — 配置列表
- GET /tenant-llm/configs/:id — 配置详情
- POST /tenant-llm/configs — 创建配置
- PUT /tenant-llm/configs/:id — 更新配置
- DELETE /tenant-llm/configs/:id — 删除配置
