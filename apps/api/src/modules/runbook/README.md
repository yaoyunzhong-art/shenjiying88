# Runbook 运维手册

> 运维手册管理服务，支持手册的创建、搜索与管理

## 功能
- 运维手册 CRUD
- 手册搜索

## 依赖
- AgentModule

## API
- POST /runbook — 创建手册
- GET /runbook — 手册列表
- GET /runbook/search — 搜索
- GET /runbook/:id — 详情
- PUT /runbook/:id — 更新
