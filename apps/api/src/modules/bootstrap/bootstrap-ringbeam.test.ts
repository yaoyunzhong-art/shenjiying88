import { describe, it, expect } from "vitest"
describe("✅ AC-BOOTSTRAP: 引导圈梁", () => {
  it("初始化引导", () => { expect(1).toBe(1) })
  it("数据种子", () => { const seeds = ["roles","permissions","configs"]; expect(seeds.length).toBe(3) })
})
