import {
  adminWorkbenchConsumerDescriptor,
  defaultRoleWorkbenchContractMap,
  defaultRoleWorkbenchContracts,
} from '@m5/types';

export const fallbackRoleWorkbenches = defaultRoleWorkbenchContracts;

export const fallbackWorkbenchMap = defaultRoleWorkbenchContractMap;
export const fallbackWorkbenchConsumerDescriptor = adminWorkbenchConsumerDescriptor;
