# P-53 部署 DevOps · 需求卡片
> 创建: 2026-07-14

## PRD 收口
- PRD-013: 部署DevOps — Deployment & DevOps (P-53)

## 树哥接单清单
- CI 门禁对齐 `RQ-53-01 / AC-53-01`
  - `.github/workflows/ci.yml`
- CD 部署链路对齐 `RQ-53-02 / AC-53-02 / AC-53-03`
  - `.github/workflows/deploy.yml`
- Docker 基础设施对齐 `RQ-53-03 / AC-53-05`
  - `Dockerfile`
  - `docker-compose.yml`
  - `infra/docker/docker-compose.dev.yml`
  - `scripts/entrypoint-*.sh`
- 环境管理对齐 `RQ-53-04`
  - `.env.example`
  - `.env.staging.example`
  - `.env.production.example`
- 回滚机制对齐 `RQ-53-05 / AC-53-04`
  - `.github/workflows/deploy.yml`（健康失败→回滚）

## 执行证据
- 主圈梁测试: `apps/api/src/modules/devops/devops-ringbeam.test.ts` (`5 tests`)
- CI: `CI` workflow（lint/typecheck/test/build + Docker build）
- CD: `Deploy` workflow（build&push + SSH 部署 + 健康检查 + 回滚）
- Docker 生产编排: `docker-compose.yml`（单一 volumes 顶层定义，包含 qdrant volumes）
- 环境模板:
  - `.env.example`
  - `.env.staging.example`
  - `.env.production.example`
- 专项审计: `docs/knowledge/p53-devops-audit.md`

## 验证记录

```bash
pnpm --dir apps/api exec vitest run src/modules/devops/devops-ringbeam.test.ts
```

## 当前缺口
- `staging -> production` 的强制双阶段 promotion 仍需进一步产品化（建议通过 GitHub Environments 审批门禁固化）
