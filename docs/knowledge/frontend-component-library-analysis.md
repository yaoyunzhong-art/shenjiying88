# 跨应用组件库可行性分析 (7/14)

> 🐜 [V17: force-run-mvl] 前端组 P1
> 分析日期: 2026-07-14 22:56

## 当前状态

### 应用概况

| 应用 | 页面数 | 关键模块 | 框架 |
|:----|:------:|:---------|:----:|
| admin-web | 185 pages / **28 stores/[id]子页** | 运营后台·门店管理·配置 | Next.js App Router |
| storefront-web | 152 pages | 门店前台·收银·预约·商品 | Next.js App Router |
| tob-web | 113 pages | 企业端·门店列表 | Next.js App Router |
| app (React Native) | ~15 screens | 移动端·收银·客服·会员 | React Native |
| mobile | ~15 screens | 店长端·报表·通知 | React Native |
| miniapp | - | 小程序 | Taro/miniapp |

### stores 页面跨应用对比

**admin-web** `stores/[id]` 共 **27个子模块** (不含layout/page):
```
analytics, audit, capability-access, cashier, devices, events,
finance, health-score, inspection, inventory, marketing, members,
operations, orders, promotions, purchasing, reconciliation,
reports, reservations, scheduling, security, service, settings,
shift-handover, staff, tenant, training
```

**storefront-web** `stores/[id]` 共 **2个子模块**:
```
edit, page
```

**tob-web** `stores/[id]` 共 **1个子模块**:
```
page
```

### 共享UI体系

| 包 | 内容 | 组件数 |
|:---|:----|:------:|
| `packages/ui` | @m5/ui 统一UI库 | **590个组件** (components/) |
| 业务面板 | Campaign/Trend/Chart/Insight/Report等 | 约30个业务Panel |
| AI面板 | Agent/Decision/Analysis等 | 约50个AI面板组件 |

**统一UI库（@m5/ui）已覆盖**：
- `components/` — 590个通用UI组件
- 业务模块: AI面板·Chart·Dashboard·License·OCR·Voice·Webhook·SSO·ImageRecognition·Monitoring·FederatedDashboard·CanaryControl
- 测试覆盖率: 组件级 `.test.tsx` 文件随组件

### 核心差距

1. **stores 页面子模块未复用** — admin-web 有27个门店子页面，storefront-web只有edit和page
2. **业务逻辑与展示绑定** — 各应用独立实现类似功能（门店配置、设备管理库存等）
3. **packages/ui 主要为面板级组件** — 没有提取 pages 级复用资产
4. **管理后台 vs 门店前台** — 同一店铺数据的不同视角，复用点集中在数据展示层

## 建议路线图

### 短期 (7/17) — 识别Top 5可复用组件

```
目标: 从3个web应用的stores模块中识别5个直接可复用的UI组件
方法:
  1. 对比 admin-web/stores/[id]/members vs storefront-web/customers/[id]
     → 门店会员列表展示组件
  2. 对比 admin-web/stores/[id]/orders vs storefront-web/orders
     → 订单通用表格/卡片组件
  3. 对比 admin-web/stores/[id]/inventory vs storefront-web/products
     → 库存/商品网格展示组件  
  4. 对比 admin-web/stores/[id]/reports vs storefront-web/insights
     → 门店报表/洞察卡片组件
  5. 对比 admin-web/stores/[id]/settings vs storefront-web/appointments/scheduling
     → 门店设置表单组件
```

### 中期 (7/20) — 提取到 @m5/ui

```
目标: 将Top 5组件提取为 @m5/ui 下的通用模块
方法:
  1. 为每个组件建立独立目录: packages/ui/src/components/StoreMemberList/
  2. 定义统一接口: data source → 数据适配层包装 API 差异
  3. 编写通用 test: 覆盖 admin-web 和 storefront-web 两种数据形态
  4. 逐步替换: admin-web 先用，storefront-web 再验证
```

### 长期 (7/27) — 所有应用统一

```
目标: admin-web + storefront-web + tob-web 共享 stores 相关50%+ UI
方法:
  1. 建立 stores 模块组件树
  2. 提取页面级布局模式 (PageLayout + 数据加载 + 子模块导航)
  3. @m5/ui 升级为 v2: 加入 "页面片段" 级复用资产
  4. 移动端 (app/mobile) 评估 RN 端可复用逻辑
```

## 风险与依赖

| 风险 | 等级 | 缓解措施 |
|:----|:----:|:---------|
| admin-web 子模块→storefront-web 功能不对齐 | 🟡 | 只提取数据展示层，不耦合业务逻辑 |
| packages/ui 组件过多(590)增加打包体积 | 🟡 | Tree-shaking + 按需导入 |
| 各应用数据层接口差异大 | 🔴 | 数据适配层模式 + 类型安全契约 |
| 移动端无法共享 web 组件 | 🟢 | 仅限 Web 应用间共享，移动端独立评估 |

## 备注

- 不修改远程仓库
- 本次分析基于V17迭代
- 🐜 [V17: force-run-mvl]

---

## 侦察兵→BI 对接分析 (Scout→Analytics)

### 现状

**侦察兵体系 (scout):**
- 5层采集标准 (L1基础→L5评价活动)
- 8平台覆盖 (抖音/美团/小红书/大众点评等)
- 3个源文件 + 2个测试, 142行代码
- API: GET scouts/cities, /venues, /venues/:id/{prices,devices,membership,reviews,activities}
- 数据落地: scout_cities, venues, competitor_prices, competitor_devices, competitor_membership, competitor_reviews 表
- 采集脚本: scripts/scout-national-collect.ts + import-national-venues.ts

**分析体系 (analytics-v2):**
- Phase-43 T173: 18+ endpoint (事件采集/CDC/队列/漏斗/留存/指标)
- 事件采集: POST /event/collect, /event/batch
- CDC: 变更数据捕获, 支持重放(tail/replay)
- Cohort 分析: 注册/跟踪/矩阵
- Funnel 漏斗: 创建/列表/详情
- Retention 留存: 生成/健康度
- Metrics 汇总: 仪表板/实时/健康报告

### 对接缺口

| 缺口 | 侦察兵状态 | 分析体系需求 | 优先级 |
|:----|:----------|:------------|:------:|
| 采集事件→分析事件 | 原始数据直接落库, 无事件抽象 | 需要统一 EventType 载体 | P2 |
| 价格趋势分析 | competitor_prices 仅有快照,无时间序列 | 需要 CDC 流式接入 analytics-v2 | P1 |
| 设备变更追踪 | 设备数据静态 | CDC 事件应捕获设备增减 | P2 |
| 评价情感趋势 | competitor_reviews 有 sentiment 字段 | 需接入 Funnel + Cohort 分析 | P1 |
| 竞品活动归因 | 活动数据独立 | 漏斗分析可关联活动→转化 | P2 |
| 实时采集监控 | 无采集成功率/延迟指标 | metrics/health 应纳入采集健康度 | P1 |

### 推荐对接方案

```
方案A: 数据库 CDC 桥接 (推荐)
  scout 表 (venues/competitor_prices/competitor_reviews)
  → PostgreSQL CDC
  → analytics-v2 CDC 流
  → 自动事件化 + 分析

方案B: API 层桥接
  scout API GET 数据
  → analytics-v2 POST /event/collect
  → 手动事件注入

推荐: 方案A (CDC), 无需修改 scout 代码, 仅需配置 CDC 流
```

### 对接路线图

| 阶段 | 内容 | 截止 |
|:----|:-----|:----:|
| 1. 数据一致性审计 | 确认 scout 表结构与 analytics-v2 实体契约匹配度 | 7/16 |
| 2. CDC 流配置 | competitor_prices + competitor_reviews → CDC 流 | 7/18 |
| 3. Funnel 分析落地 | 竞品价格变动→门店客流关联分析 | 7/21 |
| 4. 实时采集健康度 | scout 采集延迟→ analytics-v2/metrics/health | 7/23 |

---

*Generated by 🐜 pulse-force-run task · 2026-07-14 22:56*
