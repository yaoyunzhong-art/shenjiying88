# 2026-07-22/23 开发日志

## 整体概览
- 飞哥白天主导，凌晨龙虾哥静默开发
- 清单 P0-P3 全部完成，10/10 全绿，测试 197 用例全过
- 主链（收银→支付→订单→退款→财务）全部真实化，无 mock 残留
- 整体进度从 ~80% 推进至 ~95%

## P0 交易主链打穿（白天 10:12~10:46）

### P0-01 cashier 去 mock
- 删除 `paymentCodeUrl` state、硬编码 mock URL、二维码占位
- 文件：`cashier/page.tsx`

### P0-02 checkout 去 mock
- 删除 `defaultCart` 硬编码商品、`VALID_COUPONS` 本地优惠券映射
- 商品从 `listStorefrontCashierProducts()` 后端加载
- 优惠券改走 `POST /coupons/redeem`
- 文件：`checkout/page.tsx`、`storefront-transactions.ts`

### P0-03 orders 真实化
- 分析结论：代码已真实，无需改动

### P0-04 H5 支付 QR 渲染
- Image → QRCodeDisplay 组件
- 文件：`h5/payment/[orderId]/page.tsx`

### 飞哥亲自执行第二批批量去 Mock
- StorefrontScopePersistor 组件 - storeScope localStorage 持久化
- 6 个页面 scope 真实传参
- API simulateMode 门禁
- API 无 channel 时抛异常
- 测试 171+26 全绿

## P1 交易链扩展（凌晨 02:11~02:22）

### P1-01 支付状态语义统一
- SDK 新增 `TransactionOrderStatus` + `TransactionPaymentStatus` 类型约束
- 文件：`packages/sdk/src/index.ts`

### P1-02 退款闭环
- 新增 `requestStorefrontRefund` API 封装
- 订单详情页新增退款弹窗（金额输入、原因、提交流程）
- 后端 `CashierOrderCloseReason.FullRefund` 联动
- 文件：`orders/[id]/page.tsx`、`storefront-transactions.ts`、`storefront-orders.ts`

## P2 会员+finance（凌晨 02:22~02:49）

### P2-01 会员权益接入收银
- 后端新增 `GET /members/:id/balance`（余额/积分/冻结积分/优惠券数）
- checkout 页右侧展示会员权益版块
- 文件：`member.controller.ts`、`member.service.ts`、`checkout/page.tsx`、`storefront-transactions.ts`

### P2-02 finance SDK 真源
- 分析结论：代码已真实化

## P3 管理收口（凌晨 02:49~02:52）

### P3-01 状态板更新
- `master-status-board.md` 全量更新为单主线视图

### P3-02 验收模板标准化
- `_template-transaction-smoke.md` — 6 大模块 25+ 用例
- `_template-pre-release-check.md` — 5 维度 20+ 项

## 凌晨待收尾
- 会员页 mock 清理（`member-login` / `members/payment` / `member-center` / `member-churn`） — 🚧 树哥执行中
